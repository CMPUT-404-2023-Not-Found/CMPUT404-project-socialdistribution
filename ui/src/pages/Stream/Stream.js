import React from "react";
import Grid from '@mui/material/Grid';

const Stream = () => {
    return (
        <Grid item xs={8}>
            This is your stream.
        </Grid>
    );
}

export default Stream;
